[build]
  command = "npm install && npx vite build --config vite.spa.config.ts"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.19.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
